apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-aws
  namespace: nlp-cloud-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-aws
  template:
    metadata:
      labels:
        app: frontend-aws
    spec:
      containers:
        - name: frontend-aws
          image: nlp-cloud-manager/frontend-aws:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5175
          readinessProbe:
            httpGet:
              path: /
              port: 5175
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-aws
  namespace: nlp-cloud-manager
spec:
  selector:
    app: frontend-aws
  ports:
    - port: 5175
      targetPort: 5175
