apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: nlp-cloud-manager
spec:
  selector:
    app: mysql
  ports:
    - name: tcp-mysql
      port: 3306
      targetPort: 3306
      appProtocol: tcp
  clusterIP: None
